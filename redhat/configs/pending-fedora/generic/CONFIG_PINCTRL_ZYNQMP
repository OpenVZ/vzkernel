# CONFIG_PINCTRL_ZYNQMP:
# 
# This selects the pinctrl driver for Xilinx ZynqMP platform.
# This driver will query the pin information from the firmware
# and allow configuring the pins.
# Configuration can include the mux function to select on those
# pin(s)/group(s), and various pin configuration parameters
# such as pull-up, slew rate, etc.
# 
# Symbol: PINCTRL_ZYNQMP [=y]
# Type  : tristate
# Defined at drivers/pinctrl/Kconfig:326
#   Prompt: Pinctrl driver for Xilinx ZynqMP
#   Depends on: PINCTRL [=y] && ZYNQMP_FIRMWARE [=y]
#   Location:
#     -> Device Drivers
#       -> Pin controllers (PINCTRL [=y])
# Selects: PINMUX [=y] && GENERIC_PINCONF [=y]
# 
# 
# 
CONFIG_PINCTRL_ZYNQMP=y
